spring:
  kafka:
    producer:
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      transaction-id-prefix: srcntx.
    consumer:
      properties:
        isolation.level: read_committed
